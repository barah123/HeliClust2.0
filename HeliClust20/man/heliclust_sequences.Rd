% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heliclust.R
\name{heliclust_sequences}
\alias{heliclust_sequences}
\title{Hierarchical clustering for sequence / OTU data}
\usage{
heliclust_sequences(
  x,
  dist_method = "bray",
  hclust_method = "average",
  scale_data = FALSE
)
}
\arguments{
\item{x}{Numeric data frame or matrix (rows = samples, columns = variables).}

\item{dist_method}{Character; distance method passed to \code{vegan::vegdist}
(e.g. "euclidean", "bray").}

\item{hclust_method}{Linkage method for \code{stats::hclust}
(e.g. "ward.D2", "average", "complete").}

\item{scale_data}{Logical; if TRUE, standardizes variables using \code{scale()}.}
}
\value{
An object of class \code{hclust}.
}
\description{
Wrapper around \code{heliclust_dist()} and \code{stats::hclust()} for
sequence data (e.g. OTU/ASV tables).
}
\examples{
\dontrun{
  hc_seq <- heliclust_sequences(my_otu_table,
                                dist_method = "bray",
                                hclust_method = "average",
                                scale_data = FALSE)
  plot(hc_seq)
}
}
